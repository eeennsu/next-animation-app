import type { FC } from 'react';

type Props = {
    anime: unknown;
    idx: number;
}

const AnimCard: FC<Props> = ({ anime, idx }) => {

    return (
        <div>
            AnimCard
        </div>
    );
};

export default AnimCard;